# 1. Disable Directory Browsing
# Prevents people from seeing a list of all your files if they find the folder URL
Options -Indexes

# 2. Deny All Access by Default
# No one can "link" to these files directly from outside your site
deny from all

# 3. Allow Specific Extensions
# Only allow the server to serve these specific file types when requested via your shop
<FilesMatch "\.(zip|pdf|webp)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
